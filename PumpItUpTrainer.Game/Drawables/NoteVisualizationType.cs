namespace PumpItUpTrainer.Game.Drawables
{
    public enum NoteVisualizationType
    {
        /// <summary>
        /// Notes scroll like in normal Pump It Up.
        /// </summary>
        Scrolling,
        /// <summary>
        /// Notes all appear at the same time, then after a pause, all disappear at the same time.
        /// </summary>
        Freeze,
    }
}
